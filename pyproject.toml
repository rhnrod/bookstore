[tool.poetry]
name = "bookstore"
version = "0.1.0"
description = "Bookstore api"
authors = ["Rhuan <rhnrod@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
django = "^5.0.2"
djangorestframework = "^3.14.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.2"
factory-boy = "^3.3.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
